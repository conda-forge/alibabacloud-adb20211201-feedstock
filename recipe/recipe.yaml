schema_version: 1

context:
  name: alibabacloud-adb20211201
  version: "4.1.0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/alibabacloud_adb20211201-${{ version }}.tar.gz
  sha256: 9a80de0ad75cb3627a26501d056d8f9857baffdb3dc478af06264b112ad19001

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools
  run:
    - python >=${{ python_min }}
    - alibabacloud-tea-openapi >=0.4.5,<1.0.0
    - darabonba-core >=1.0.0,<2.0.0

tests:
  - python:
      imports:
        - alibabacloud_adb20211201
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  summary: Alibaba Cloud adb (20211201) SDK Library for Python
  license: Apache-2.0
  license_file: LICENSE
  homepage: https://github.com/aliyun/alibabacloud-python-sdk

extra:
  recipe-maintainers:
    - xylar
